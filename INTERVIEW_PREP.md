# INTERVIEW_PREP — multik_bot как эталонный проект для собеса ML / LLM Engineer

> Всё в этом файле привязано к реальному коду проекта (файлы + параметры сверены),
> а не к общим учебникам. Цифры eval — из настоящего прогона harness'а.

## 0. Как пользоваться

1. Выучи раздел **2 (Питч)** — с него начинается любой разговор про проект.
2. Прогони раздел **4 (50 вопросов)** вслух: отвечай 30–60 сек, глядя в код.
3. Раздел **5 (слабые места)** — то, чем тебя будут подлавливать; там готовые ответы.
4. Раздел **6 (карточка цифр)** — выучи наизусть, интервьюер любит конкретику.

---

## 1. Что за проект (одним абзацем)

**multik_bot** — production-grade RAG-система для ФНС РФ: офлайн (air-gapped)
вопросно-ответный ассистент по нормативно-правовой базе (79-ФЗ, 58-ФЗ, указы,
постановления). Полный ML-пайплайн: скачивание и парсинг НПА → structure-aware
чанкинг по токенам → эмбеддинги FRIDA → Qdrant → гибридный поиск (dense+BM25) →
cross-encoder реранкинг → LLM-генерация со стримингом → GraphRAG-слой → LoRA-адаптер
→ Docker/air-gapped деплой на GPU 6 ГБ.

## 2. Питч проекта (Проблема → Ограничения → Решение → Результат → Что улучшил бы)

**Проблема.** Сотруднику ФНС нужно отвечать на вопросы по нормативке быстро и
юридически точно. Обычная LLM галлюцинирует, а внешние API (OpenAI/GigaChat-cloud)
нельзя использовать в закрытом контуре ФНС.

**Ограничения.**
- Air-gapped: нет интернета, всё локально (`HF_HUB_OFFLINE=1`, локальные модели, Docker из кэша).
- Железо: GPU 6 ГБ VRAM — Ollama с моделью YandexGPT-5-Lite-8B (Q4_K_M) занимает ~5590 MiB,
  поэтому эмбеддер и реранкер принудительно на CPU (`device='cpu'` в `engine_rag.py`).
- Контекст всего 6144 токена (`PARAMETER num_ctx 6144` в `Modelfile`).

**Решение (слоями).**
1. **Ingestion**: `publication.pravo.gov.ru` API (exact/fail-closed выбор документа) + legacy
   IPS + Playwright HTML→PDF; HTML→Markdown (BeautifulSoup+pandoc) → структура (linear/tree/records).
2. **Chunking**: `legal_chunker.py` — нарезка по статье/пункту с реальной FRIDA-токенизацией
   (TARGET=450, MAX=512), заголовок статьи в каждом чанке, статьи не смешиваются.
3. **Index**: FRIDA (1536-dim, CLS-pooling) → Qdrant `fns_collection` (Cosine, HNSW m=16/ef=100).
4. **Retrieval**: гибрид dense (Qdrant) + BM25 (`rank_bm25` + `SnowballStemmer('russian')`)
   через weighted RRF (0.65/0.45, k=30) → cross-encoder реранк top-10 → top-5.
5. **Generation**: Ollama (YandexGPT-5-Lite), temp=0, seed=42, строгий grounding-промпт,
   режимы COMPACT / TREE_SUMMARIZE / REFINE, SSE-стриминг, отказ `БАЗА_ПУСТА`.
6. **GraphRAG**: LLM-экстракция сущностей/связей → networkx + Louvain → саммари кластеров.
7. **Fine-tune**: LoRA/QLoRA (Unsloth) на Llama-3.2-3B под домен ФНС.
8. **Serving**: FastAPI + Redis-кэш (TTL 86400), middleware request_id, глобальный error handler.

**Результат (факты из кода и прогонов).**
- End-to-end ~34 сек: ~16 сек реранкинг на CPU + ~18 сек генерация; ~15 ток/сек.
- 4410 чанков в Qdrant, 4294 уникальных id; эмбеддинг 1536-dim.
- На реальном прогоне harness'а (24 вопроса): hybrid > dense по R@3 (0.583→0.750) и MRR (0.556→0.637).
- 400+ автотестов (pytest, `asyncio_mode=auto`).

**Что улучшил бы (и уже начал).**
- Замерил качество поиска офлайн-харнессом (`scripts/eval_retrieval.py`): recall@k/MRR/nDCG + ablation.
- Калибровка весов RRF и порога реранка по этому harness'у.
- Реранкер с CPU → GPU (или distill/уменьшить модель): ~15 сек на CPU — осознанный trade-off; GPU занята LLM (6 ГБ), cross-encoder точнее bi-encoder. Альтернатива — distill / модель поменьше.
- Unified-токенайзер для подсчёта токенов (сейчас в движке эвристика `len/4`).
- Инкрементальная переиндексация вместо полного пересоздания коллекции.

## 3. Карта системы (файлы, на которые ссылаться в ответах)

| Слой | Файл |
|------|------|
| FastAPI + кэш + роутинг | `app/rag/main_api.py` |
| Ядро retrieval/rerank/LLM | `app/rag/engine_rag.py` (класс `RerankedEngine`) |
| Источники ответа (дедуп) | `app/rag/sources.py` (`collect_sources`) |
| Чанкер НПА | `app/chunking/legal_chunker.py` |
| Индексация в Qdrant | `app/chunking/create_index_qdrant_chunks.py` |
| Ingestion HTML→MD→структура | `app/ingestion/*`, `app/publication_api.py`, `app/pravo_resolver.py` |
| Графики (ECharts + Pydantic) | `app/rag/chart_engine.py` |
| GraphRAG | `GraphRAG/script.py`, `GraphRAG/build_graph_communities.py`, `GraphRAG/make_summaries.py` |
| LoRA-обучение | `scripts/train_unsloth.py`, `lora_fns_adapter_3b_v6/` |
| Eval (этот проект) | `scripts/eval_retrieval.py`, `scripts/build_eval_set.py` |
| Инфра | `Dockerfile`, `docker-compose.yml`, `Modelfile` |

## 4. Реальные цифры eval (ablation)

Прогон: 24 вопроса (`llm-gen`), корпус — живой `fns_collection` (4294 чанка),
match-level `segment`, CPU. Команда:

```bash
venv/bin/python scripts/build_eval_set.py --mode llm-gen --n 24 --out eval/qrels.jsonl
venv/bin/python scripts/eval_retrieval.py --qrels eval/qrels.jsonl \
    --configs dense,bm25,hybrid,hybrid_rerank --top-k 1,3,5,10 --out eval/
```

| config | R@1 | R@3 | R@5 | R@10 | MRR@10 | nDCG@10 | lat_mean | lat_p95 |
|---|---|---|---|---|---|---|---|---|
| dense | 0.458 | 0.583 | 0.667 | 0.833 | 0.556 | 0.621 | 260 ms | 372 ms |
| bm25 | 0.458 | 0.583 | 0.792 | 0.875 | 0.578 | 0.649 | 19 ms | 33 ms |
| hybrid | 0.500 | 0.750 | 0.833 | 0.875 | 0.637 | 0.696 | 250 ms | 336 ms |
| hybrid_rerank | 0.500 | 0.792 | 0.833 | 0.875 | 0.647 | 0.703 | 14224 ms | 16177 ms |

**Как читать (готовые тезисы для собеса):**
- Гибрид заметно лучше dense: R@3 +0.167, MRR +0.081 → редкие термины/номера норм ловит BM25.
- BM25 один даёт R@10 0.875 при 19 мс — в 13× быстрее dense и сильнее его на верхних k.
- Реранкер XLM-R-large даёт +0.042 R@3 за ~14 сек (на 24 вопросах) — осознанный trade-off:
  CPU вместо GPU ради освобождения VRAM для LLM. Альтернатива — GPU-реранкер (~500 мс),
  но не влезает в 6 ГБ. На большем наборе (99 вопросов, 4.1) прирост ещё заметнее.
- Честная оговорка: gold «слабый» (вопросы сгенерированы из текста чанка) → цифры завышены,
  нужен ручной золотой набор (режим `from-questions`).

### 4.1. Расширенный прогон (99 вопросов)

Прогон на 99 вопросах (`--mode llm-gen`, `eval/qrels100.jsonl`, все 99 id валидны в корпусе).
Команда: `eval_retrieval.py --qrels eval/qrels100.jsonl --out eval/run100`.

| config | R@1 | R@3 | R@5 | R@10 | MRR@10 | nDCG@10 | lat_mean | lat_p95 |
|---|---|---|---|---|---|---|---|---|
| dense | 0.444 | 0.717 | 0.798 | 0.859 | 0.591 | 0.657 | 298 ms | 384 ms |
| bm25 | 0.475 | 0.586 | 0.657 | 0.768 | 0.554 | 0.604 | 16 ms | 26 ms |
| hybrid | 0.475 | 0.737 | 0.818 | 0.909 | 0.621 | 0.691 | 312 ms | 421 ms |
| hybrid_rerank | **0.646** | **0.828** | **0.879** | 0.909 | **0.745** | **0.785** | 15186.2 | 21922.8 |

**Вывод на 99 вопросах (устойчивее, чем на 24):** лучшая конфигурация — **hybrid_rerank**:
R@1 **0.646** (+0.171 к hybrid), R@3 **0.828** (+0.091), R@5 **0.879** (+0.061). Это осознанный
trade-off: CPU-реранкинг (~15 сек) вместо GPU, т.к. VRAM занята LLM (6 ГБ). Cross-encoder
видит запрос и документ вместе — точнее, чем bi-encoder. На GPU было бы ~500 мс.

Если без реранка — **hybrid** (R@3 0.737, R@10 0.909, MRR 0.621) против dense (+0.020 R@3,
+0.050 R@10). BM25 один слабее dense на этом наборе (R@10 0.768 vs 0.859), но в ~19× быстрее.
Разница между 24 и 99 наглядно показывает, почему маленькая выборка смещает выводы
(на 24 было много преамбул) — нужен репрезентативный gold.

---

## 5. 50 вопросов с ответами (по коду)

### A. Данные и чанкинг (`app/chunking/legal_chunker.py`)

**1. Почему structure-aware чанкинг, а не fixed-size по символам?**
Потому что единица смысла в НПА — статья/пункт. Fixed-size рвёт норму пополам,
и retrieval находит «половину правила». Чанкер режет по `article/paragraph/subparagraph`,
статья — жёсткая логическая граница (`TARGET_TOKENS=450`, `MAX_TOKENS=512`).

**2. Почему разные статьи не смешиваются в одном чанке?**
Иначе embedding размывается двумя темами, а LLM-контекст смешивает нормы и провоцирует
неверный ответ. `_build_chunks_for_segment` не пересекает границы статей.

**3. Зачем заголовок статьи в каждом чанке?**
Каждый чанк должен быть самодостаточным при отдаче в LLM (чанки ранжируются и
собираются независимо). Заголовок даёт контекст нормы без соседних чанков.

**4. Почему лимиты 450/512 и как считается длина?**
Считается реальным FRIDA-токенизатором (`AutoTokenizer.from_pretrained(hf_cache/FRIDA)`,
кэш через `lru_cache`), не по символам. 512 — потолок эмбеддера (`max_seq_length=512`),
450 — целевой размер с запасом под метаданные/заголовок.

**5. Что происходит с блоком «(В редакции …)»?**
`_EDITORIAL_RE` ловит редакционные пометки преамбулы; их НЕ дробят на много чанков —
оставляют единым блоком (иначе получаешь десятки мусорных «поисковых» чанков).

**6. Зачем `MIN_CHUNK_TOKENS=40`?**
Хвостовые обрывки короче 40 токенов приклеиваются к предыдущему чанку — иначе
одиночные «огрызки» засоряют индекс и BM25 получает бессмысленный документ.

**7. Как режется элемент длиннее MAX_TOKENS?**
`_truncate_to_limit`/`_split_oversized`: сначала по предложениям (regex по `.;!?`),
если не влезает — по словам, НИКОГДА не разрывая слово (без « …» и без обрубков).

**8. Что за structure JSON (linear/tree/records)?**
`markdown_structure_parser.py` строит три представления: `linear` (точный порядок),
`tree` (иерархия раздел→статья→пункт), `records` (плоский вход для чанкера).
Разделение слоёв = чанкер не знает ни о PDF, ни о pandoc — только о `records`.

**9. Почему pandoc, а не свой HTML-парсер?**
Pandoc даёт стабильный GFM; свой парсер на каждом НПА пришлось бы дообучать под вёрстку.
BeautifulSoup используется только на санитизации ДО pandoc.

**10. Откуда берутся фото в ответах?**
`employees.txt` (ФИО→вариации) + `images_cache/`; `_find_photo` ищет упоминание ФИО
в ответе и подставляет фото, иначе — `local_img` из лучшего чанка.

### B. Эмбеддинги и векторный поиск (`app/rag/engine_rag.py`, `create_index_qdrant_chunks.py`)

**11. Что за эмбеддер и почему он?**
FRIDA (Sber, `T5EncoderModel`, 1536-dim, русскоязычный) — нужен качественный
русский bi-encoder в air-gapped; загрузка через `models.Transformer` + `models.Pooling`.

**12. Почему CLS-pooling, а не mean?**
У FRIDA в конфиге `pooling_mode_cls_token: true`; так снята головная модель.
Соблюдаю дистрибутив — иначе эмбеддинги не совпадут с индексированными.

**13. Зачем префиксы `search_query:` / `search_document:`?**
Ассиметричный поиск: `_get_query_embedding` кодирует `search_query: {q}`, документы —
`search_document: {t}`. Модель обучена в этих «инструкциях», префиксы двигают
query и doc в согласованное пространство.

**14. В проде эмбеддер без модуля Normalize — это ок?**
Да: `SentenceTransformer(modules=[Transformer, Pooling])` (модуль `2_Normalize` не подключён),
но Qdrant использует `Cosine`, который нормирует при сравнении. Нормировку добавил
только в eval при ручном косинусе — чтобы результат совпал с Qdrant.

**15. Почему 1536-dim и как с этим связан Qdrant?**
`get_word_embedding_dimension()==1536`; коллекция создана на size=1536 — менять размерность
нельзя без полной переиндексации (иначе размерность векторов не совпадёт).

**16. Cosine vs dot-product?**
Qdrant настроен на `Cosine`. Для нормированных векторов cosine == dot, но т.к. векторы
НЕ нормированы на этапе модели, выбирать dot было бы некорректно.

**17. Что такое HNSW и параметры `m=16`, `ef_construct=100`?**
Приближённый граф ближайших соседей: `m` — число связей на узел (память vs recall),
`ef_construct` — ширина поиска при построении (качество индекса). `ef_search` —
ширина на запросе (latency vs recall). У нас на 4410 точках — HNSW достаточен.

**18. Зачем `excluded_embed_metadata_keys`?**
`id`, `source_url`, `local_img`, `text` исключены из embedding — служебные поля не должны
размывать семантику. В embedding идёт контент + `title` (+ `_em_input`-маркеры).

### C. Retrieval, гибрид, реранк (`engine_rag.py: RerankedEngine`)

**19. Что такое BM25 и что делают k1/b?**
Мешок слов с насыщением TF (`k1`) и нормировкой длины документа (`b`). Лучше TF-IDF на
длинных документах. Реализация — `rank_bm25.BM25Okapi`.

**20. Почему для BM25 стемминг, а для dense — нет?**
BM25 ищет по строкам → нужны «общие корни» (`SnowballStemmer('russian')`),
чтобы «служащий/служащего» совпали. Dense работает на семантике, стемминг ему не нужен.

**21. Почему слово «не» не стеммится?**
Отрицание меняет смысл нормы («не подлежит» ≠ «подлежит»). В `_tokenize` есть спец-кейс:
если `w == "не"` — добавляем как есть, остальное стеммим. Плюс `NEGATIVE_PATTERNS`.

**22. Почему BM25 особенно полезен на НПА?**
Точные термины, аббревиатуры, номера («79-ФЗ», «НДС», «ст. 217») — редкие токены,
где dense может «плыть», а BM25 ловит буквально.

**23. Что такое RRF и его формула здесь?**
Reciprocal Rank Fusion: для каждого списка `score += weight / (k + rank + 1)`.
В коде: vector — `VECTOR_WEIGHT=0.65`, bm25 — `BM25_WEIGHT=0.45`, `k=30`
(`_reciprocal_rank_fusion`).

**24. Почему объединяем по рангам, а не по скорам?**
Скоры dense и BM25 в разных шкалах (cosine vs наборная функция) — складывать нельзя.
Ранги же сравнимы по построению. Это стандартное преимущество RRF.

**25. Что значат 0.65/0.45 и 30; это «правильные» числа?**
`k=30` — сглаживание хвоста рангов; веса — приоритет dense с учётом вклада BM25.
Честный ответ: подбирались эмпирически; теперь это можно калибровать harness'ом
(ablation показал, что гибрид > dense, а вклад BM25 существенный).

**26. Почему vector top-K = 30 и `BM25_TOP_K = 30`?**
`RerankedEngine(initial_top_k=30)` для retriever'а и `BM25_TOP_K=30` — компромисс:
достаточно кандидатов для фьюжна, но не тормозим реранк. Дальше берём top-10 на реранк.

**27. Чем cross-encoder отличается от bi-encoder?**
Bi-encoder кодирует запрос и документ независимо (быстро, но грубее) — это FRIDA.
Cross-encoder подаёт пару (query, doc) в одну модель, видит их взаимодействие (точнее,
но дорого) — это `SentenceTransformerRerank` (XLM-R).

**28. Почему реранк только top-10?**
Cross-encoder ~O(n) тяжёлых forward'ов. Реранк 10 кандидатов даёт качество почти как
реранк всего, но укладывается в бюджет. Прогон: 10 пар/запрос ≈ 14 сек на CPU.

**29. Что с `SCORE_THRESHOLD=0.05`?**
В коде он вычисляется, но строка фильтра ЗАКОММЕНТИРОВАНА (`final_nodes = top_5_reranked`).
Лог при этом пишет «прошли порог». Причина отключения: скоры cross-encoder не калиброваны
под 0.05 и резали валидные нормы. Правильно — калибровать порог по валидационному набору.

**30. Как нормализуются запросы (`QUERY_REPLACEMENTS`)?**
`lower()` + замены длинными паттернами первыми (`_normalize_query`): «госслужащий»→
«гражданский служащий», «увольнение»→«прекращение служебного контракта», «ё»→«е» и т.д.
Применяется и к dense, и к BM25. Риск: ручной словарь — доменный хардкод, нужно
сопровождать; авто-вариант — query-expansion LLM'ом.

### D. LLM, инференс, промптинг (`engine_rag.py`, `Modelfile`, `docker-compose.yml`)

**31. Какая LLM и как развёрнута?**
YandexGPT-5-Lite-8B-Instruct (в реестре Минцифры) в GGUF Q4_K_M через Ollama.
Ollama — отдельный контейнер с GPU passthrough (NVIDIA), API-контейнер GPU не видит.

**32. Почему Q4_K_M и что при этом теряем?**
4-битная квантизация ≈ в 4 раза меньше VRAM при умеренной потере качества; `_M` —
смешанная (часть слоёв точнее). Для RAG с жёстким grounding это приемлемо; теряем
немного в «свободной» генерации, что нам и не нужно.

**33. Что такое KV-cache и зачем `OLLAMA_KV_CACHE_TYPE=q8_0`?**
KV-cache хранит ключи/значения attention для уже сгенерированных токенов, чтобы не
пересчитывать. Float16 → q8_0 сжимает его вдвое и позволяет влезть в 6144 контекста
на 6 ГБ. Минус — микро-потеря точности attention.

**34. Почему `num_ctx=6144` и как контролируется бюджет?**
Ограничение VRAM: 6144 токена (Modelfile) + KV q8_0 ≈ 5590 MiB. Бюджет держим
top-5 чанками и логированием «на вход улетело ~N токенов» (`logging_token_generator`).
⚠️ В движке подсчёт — эвристика `len/4` (в чанкере — реальный токенизатор): расхождение, пункт на улучшение.

**35. Почему `temperature=0` и `seed=42`?**
Нужна детерминированная, воспроизводимая выдача для юридических норм: никакой
«творческости». `seed` фиксирует воспроизводимость.

**36. Как боретесь с галлюцинациями?**
Три уровня: (1) strict-промпт `_QA_PROMPT_STR` — «отвечай ТОЛЬКО по контексту, не выдумывай»,
начинать с «**Ответ:**»; (2) отказ `БАЗА_ПУСТА` при отсутствии нормы
(`_EMPTY_RESPONSE_RE`); (3) retrieval-фильтры — `NEGATIVE_PATTERNS` («за исключением»,
«не подлежит») и порог реранка (сейчас отключён).

**37. Что за response modes и как выбираются?**
`_select_response_mode`: запросы на сравнение/обобщение («сравни», «обобщи», «анализ»)
→ `TREE_SUMMARIZE`; остальное → `COMPACT`; есть `REFINE`. Это разные синтезаторы
LlamaIndex: COMPACT — один проход, TREE — иерархическая суммаризация (для глобальных вопросов).

**38. Что такое SSE-стриминг и зачем?**
Server-Sent Events: `StreamingResponse(media_type="text/event-stream")` отдаёт токены
по мере генерации (`response_gen`). Убирает «психологическое ожидание» — первый токен
приходит раньше, чем готов весь ответ.

**39. Как чарт-контур защищён от невалидного JSON?**
`DynamicChartEngine` (Pydantic `EChartsConfig`): LLM отдаёт `[CHART_JSON]{...}[/CHART_JSON]`,
`validate_and_parse` санитайзит и валидирует схему; при ошибке — `chart_error` + фолбэк
на текстовый ответ. Данные не выдумываются: при отсутствии чисел — «БАЗА_ПУСТА».

**40. Почему `OLLAMA_NUM_PARALLEL=1` и `KEEP_ALIVE=-1`?**
`NUM_PARALLEL=1` — не даём параллельным сессиям раздуть VRAM и получить CUDA OOM.
`KEEP_ALIVE=-1` — модель постоянно в VRAM (нет холодного старта). Плюс `FLASH_ATTENTION=1`.

### E. Fine-tuning (`scripts/train_unsloth.py`, `lora_fns_adapter_3b_v6/`)

**41. LoRA: что такое rank и alpha?**
LoRA добавляет к весам низкоранговую добавку `ΔW = B·A` (r маленький). `rank=16` —
размерность добавки, `alpha=32` — масштаб (`scaling = alpha/r = 2`). Больше alpha →
сильнее влияние адаптера. В коде: `r=16, lora_alpha=32`.

**42. QLoRA / 4-bit: NF4 и double quant?**
Базовая модель грузится в 4-бит (`load_in_4bit=True`, NF4 — квантиль-нормальное
распределение под веса), обучаются только LoRA-адаптеры. Это позволяет тюнить на
маленькой VRAM. Unsloth оптимизирует память/скорость.

**43. Почему `target_modules` все проекции?**
`q,k,v,o,gate,up,down` — адаптируем и attention, и MLP: шире влияние при том же rank.
Если не хватает VRAM/данных — можно ограничиться `q,v`.

**44. Как боролись с overfitting?**
`lora_dropout=0.05`, `weight_decay=0.1`, `learning_rate=3e-5`, 3 эпохи,
`warmup_steps=20`, cosine scheduler. Датасет маленький (`fns_100_examples.jsonl`),
поэтому регуляризация важнее «мощности».

**45. SFT vs DPO/ORPO/GRPO — разница?**
SFT — обучение на «правильных» ответах (у нас это). DPO/ORPO — предпочтения пар
(chosen/rejected) без reward-модели. GRPO/PPO — RL с наградой. В кэше проекта есть
готовые тренеры Unsloth (DPO/ORPO/KTO/GRPO/PPO/RLOO) — следующий шаг после SFT.

### F. GraphRAG (`GraphRAG/`)

**46. Как строится граф?**
`script.py` прогоняет чанки через LLM → извлекает сущности и отношения → `graph_nodes.jsonl`.
`build_graph_communities.py` собирает `networkx.DiGraph` (узлы, рёбра с весами,
типы: article/law/authority/normative_act/concept).

**47. Что даёт Louvain?**
Community detection по модулярности: группирует связанные сущности в кластеры (`communities.json`),
дальше `make_summaries.py` делает LLM-саммари каждого кластера (`graph_summaries.jsonl`).
Это основа для «глобальных» вопросов, где нет ответа в одном чанке.

**48. Работает ли entity boost в текущем движке?**
Честно: `_load_graph_entities` и `_extract_query_entities` в `app/rag/engine_rag.py` есть,
но в активном `_sync_query` бонус НЕ применяется (аддитивный entity boost живёт в
legacy-движках `engines/engine_rag_gigachat.py`, `engine_rag_qdrant_15052026.py`).
Это надо знать, чтобы не проколоться на вопросе «покажи, где буст работает».

### G. Serving / MLOps (`app/rag/main_api.py`, Docker)

**49. Почему CPU-bound работа уходит в `asyncio.to_thread`?**
`_sync_query` синхронный и грузит CPU (FRIDA/BM25/реранк). Если звать напрямую в async —
заблокируем event loop и сервер перестанет принимать запросы. `asyncio.to_thread`
уносит это в thread-pool. (Плюс GIL: реальный параллелизм даёт только torch, освобождающий GIL.)

**50. Как устроено кэширование и что НЕ кэшируется?**
Redis, ключ = `normalized_query.lower()`, TTL 86400. Не кэшируются ответы с «error»/
«база_пуста»/«информация отсутствует» — иначе залипнет заглушка. Мультик-ветка (без ФНС-ключевиков)
идёт мимо RAG напрямую в Ollama.

---

## 6. Слабые места проекта — что подловит интервьюер и как отвечать

| # | Слабое место | Готовый ответ |
|---|---|---|
| 1 | **Не было метрик качества** | «Было — теперь закрыто: `scripts/eval_retrieval.py` считает recall@k/MRR/nDCG + ablation. Реальный прогон показал hybrid > dense (R@3 0.583→0.750). Реренкер на 99 вопросах даёт +0.091 R@3, +0.061 R@5 за ~15 сек — осознанный trade-off: CPU вместо GPU ради освобождения VRAM для LLM». |
| 2 | `SCORE_THRESHOLD=0.05` не применяется (закомментирован) | «Знаю. Скоры cross-encoder не калиброваны под фиксированный 0.05 и резали валидные нормы — отключил осознанно. Правильный путь: подобрать порог по валидационному набору (harness готов)». |
| 3 | Разный подсчёт токенов: чанкер — реальный FRIDA-токенизатор, движок — эвристика `len/4` | «Да, расхождение есть. Точный токенайзер в горячем пути дорогой; лечится кэшем токенизатора и бюджетированием по нему». |
| 4 | Entity boost не подключён в активном движке | «Функции есть (`_extract_query_entities`), но в `_sync_query` не применяются — boost остался в legacy-движках. Либо включить, либо убрать из README». |
| 5 | Реранк ~15 сек на CPU — не дорого, а трейд-офф | «Cross-encoder точнее bi-encoder (видит пару запрос+документ вместе). На 99 вопросах: +0.091 R@3, +0.061 R@5. CPU — осознанно: GPU (6 ГБ) отдана LLM. Альтернатива — distill-модель / GPU (~500 мс)». |
| 6 | `asyncio.run()` внутри синхронного `_sync_query` (debug-дамп) | «Хак для debug-дампа, в прод-режиме ветка не исполняется (DEBUG_MODE=False). Правильно — вынести дамп наружу/в async-обёртку». |
| 7 | `bm25_cache.pkl` ~40 МБ через `pickle` | «Кэш пересобирается при невалидности; pickle — вопрос безопасности и нестабильности формата. Замена — JSONL/parquet или in-memory rebuild с прогрессом». |
| 8 | 8 почти одинаковых движков в `engines/` + `.bak`/`_archive` | «История экспериментов (e5 vs sber vs gigachat). В проде активен только `app/rag/engine_rag.py`; чистка — техдолг». |
| 9 | Веса RRF/пороги не тюнились | «Подбирались эмпирически; теперь есть harness — можно grid/bayes-поиск по весам и match-level». |
| 10 | Eval-gold синтетический («слабый») | «Понимаю ограничение: вопросы сгенерированы из чанков → завышают recall. Для честных цифр — режим `from-questions` + ручная проверка флага `needs_review`». |

---

## 7. Карточка цифр (выучить)

| Метрика | Значение |
|---|---|
| VRAM | 6 ГБ; Ollama ~5590 MiB, API без GPU |
| Контекст LLM | 6144 токена (`num_ctx`), KV-cache `q8_0`, temp=0, seed=42 |
| Модель | YandexGPT-5-Lite-8B-Instruct, GGUF **Q4_K_M** |
| Скорость генерации | ~15 ток/сек |
| End-to-end | ~34 сек (~16 сек реранк CPU + ~18 сек генерация) |
| Эмбеддинг | FRIDA, **1536-dim**, CLS-pooling, CPU |
| Векторная БД | Qdrant `fns_collection`: **4410** точек (4294 uniq), Cosine, HNSW m=16/ef=100 |
| Чанкинг | TARGET=**450**, MAX=**512** токенов, MIN=40, FRIDA-токенизатор |
| Retrieval | dense top-30 + BM25 top-30 → RRF (0.65/0.45, k=30) → rerank top-10 → **top-5** |
| Реранкер | XLM-RoBERTa cross-encoder (~2.2 ГБ), CPU |
| Кэш | Redis, ключ=lowercase query, TTL **86400** сек |
| LoRA | Llama-3.2-3B-4bit, r=16, alpha=32, target=q,k,v,o,gate,up,down, adamw_8bit, 3 эпохи |
| Тесты | 400+ pytest (`asyncio_mode=auto`) |

**Eval ablation (99 вопросов):** hybrid_rerank — лучшая точность (R@1 **0.646**, R@3 **0.828**,
R@5 **0.879**); hybrid — баланс (R@3 0.737, R@10 0.909, 312 мс); BM25 — самый быстрый
(16 мс, R@1 0.475). Реренкер: +0.171 R@1, +0.091 R@3, +0.061 R@5 за ~15 сек —
осознанный trade-off CPU vs GPU.

---

## 8. Быстрая самопроверка (ответь за 30 сек)

1. Почему LLM не видит GPU в API-контейнере?
2. Что делает `_tokenize` со словом «не» и зачем?
3. Формула RRF и роль `k=30`.
4. Почему `Cosine` при ненормированных векторах?
5. Как считаешь бюджет токенов и в чём текущее расхождение?
6. Чем DPO/ORPO/GRPO отличаются от SFT?
7. Почему Louvain нужен для «глобальных» вопросов?
8. Что показывает ablation: hybrid vs dense, bm25 vs dense?
9. Что НЕ кэшируется в Redis и почему?
10. Какие 3 вещи улучшил бы в первую очередь (и почему)?

> Ответ на №10 (сильная концовка): «(1) калибровка весов RRF и порога реранка по harness'у;
> (2) перенос реранкера на GPU или distillation — ~15 сек на CPU оправданы точностью (R@3 0.828, +0.091 к hybrid), но на GPU было бы ~500 мс;
> (3) золотой набор из реальных вопросов пользователей вместо синтетического — иначе
> метрики оптимистичны».
